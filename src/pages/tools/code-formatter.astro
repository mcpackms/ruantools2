---
// 页面元数据
import CodeFormatter from '../../components/CodeFormatter.vue';

const pageTitle = "多语言代码格式化工具 - RuanTools";
const description = "一个强大的代码格式化工具，支持 JSON、Python、Java、C/C++、HTML、CSS、JavaScript 等多种语言的代码美化和语法高亮。";
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content={description} />
  <title>{pageTitle}</title>
  
  <!-- 基础样式 -->
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
      min-height: 100vh;
      color: #1e293b;
    }

    /* 自定义滚动条 */
    ::-webkit-scrollbar {
      width: 10px;
      height: 10px;
    }

    ::-webkit-scrollbar-track {
      background: #f1f5f9;
      border-radius: 5px;
    }

    ::-webkit-scrollbar-thumb {
      background: #cbd5e1;
      border-radius: 5px;
    }

    ::-webkit-scrollbar-thumb:hover {
      background: #94a3b8;
    }

    /* 代码高亮样式 */
    .json-key { color: #f92672; }
    .json-string { color: #a6e22e; }
    .json-number { color: #ae81ff; }
    .json-boolean { color: #fd971f; }
    .keyword { color: #66d9ef; font-weight: bold; }
    .comment { color: #75715e; font-style: italic; }
    .string { color: #e6db74; }
    .number { color: #ae81ff; }
    
    /* 容器样式 */
    .app-container {
      max-width: 100%;
      min-height: 100vh;
    }
    
    /* 头部样式 */
    .app-header {
      text-align: center;
      padding: 30px 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      margin-bottom: 30px;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }
    
    .app-header h1 {
      margin: 0;
      font-size: 2.25rem;
      font-weight: 700;
      margin-bottom: 10px;
    }
    
    .app-header p {
      margin: 0;
      font-size: 1.1rem;
      opacity: 0.9;
      max-width: 600px;
      margin: 0 auto;
      line-height: 1.6;
    }
    
    /* 主内容区域 */
    .main-content {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 20px 40px;
    }
    
    /* 底部样式 */
    .app-footer {
      text-align: center;
      padding: 30px 20px;
      margin-top: 50px;
      background-color: white;
      border-top: 1px solid #e2e8f0;
      color: #6b7280;
      font-size: 0.875rem;
    }
    
    .footer-note {
      font-size: 0.75rem;
      color: #9ca3af;
      margin-top: 8px;
    }
    
    /* 响应式调整 */
    @media (max-width: 768px) {
      .app-header {
        padding: 20px 15px;
      }
      
      .app-header h1 {
        font-size: 1.75rem;
      }
      
      .main-content {
        padding: 0 15px 30px;
      }
      
      .app-footer {
        padding: 20px 15px;
      }
    }
    
    @media (max-width: 480px) {
      .app-header h1 {
        font-size: 1.5rem;
      }
      
      .app-header p {
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="app-container">
    <!-- 头部 -->
    <header class="app-header">
      <h1>多语言代码格式化工具</h1>
      <p>支持 JSON、Python、Java、C/C++、HTML、CSS、JavaScript、XML 等多种编程语言的代码格式化和语法高亮</p>
    </header>

    <!-- 主内容区 -->
    <main class="main-content">
      <CodeFormatter client:load />
    </main>

    <!-- 底部信息 -->
    <footer class="app-footer">
      <p>&copy; 2024 RuanTools - 专业的在线工具集合</p>
      <p class="footer-note">
        注意：所有格式化操作均在浏览器本地完成，无需网络传输，保障您的代码安全
      </p>
    </footer>
  </div>
</body>
</html>
