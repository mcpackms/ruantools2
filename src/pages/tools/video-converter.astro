---
const pageTitle = "视频格式转换 - RuanTools";
const description = "在线视频格式转换工具，支持 MP4、WebM、MOV、AVI、MKV 格式互转";
import VideoConverter from '../../components/VideoConverter.vue';
---

<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{pageTitle}</title>
    
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        line-height: 1.6;
        color: #333;
        background: #f5f5f5;
        min-height: 100vh;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
      }

      .header {
        text-align: center;
        margin-bottom: 30px;
      }

      .header h1 {
        font-size: 2rem;
        color: #333;
        margin-bottom: 10px;
      }

      .header p {
        color: #666;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header class="header">
        <h1>视频格式转换</h1>
        <p>使用 Vue.js + FFmpeg.wasm 实现的视频格式转换工具</p>
      </header>
      
      <VideoConverter client:load />
    </div>
  </body>
</html>
