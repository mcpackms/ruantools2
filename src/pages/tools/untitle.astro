---
// src/pages/tools/aes-zero-padding.astro
import Layout from '../../layouts/Layout.astro';
import AESZeroPadding from '../../components/AESZeroPadding.jsx';

const pageTitle = 'AES 加密解密工具 (零填充)';
const description = '支持 AES-128-CBC 零填充模式的在线加密解密工具，密钥与IV相同，数据在本地浏览器处理';
---

<Layout title={`${pageTitle} | 软糖工具`}>
  <div class="container mx-auto px-4 py-8 max-w-4xl">
    <!-- 页面标题和描述 -->
    <div class="mb-8">
      <h1 class="text-3xl md:text-4xl font-bold text-gray-900 dark:text-gray-100 mb-4">
        {pageTitle}
      </h1>
      <p class="text-lg text-gray-600 dark:text-gray-400">
        {description}
      </p>
    </div>

    <!-- 工具主界面 -->
    <div class="mb-8">
      <AESZeroPadding client:load />
    </div>

    <!-- 使用说明 -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
      <!-- 加密说明 -->
      <div class="bg-gradient-to-br from-blue-50 to-indigo-50 dark:from-gray-900 dark:to-gray-800 rounded-2xl p-6">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 bg-blue-100 dark:bg-blue-900 rounded-xl flex items-center justify-center">
            <span class="text-2xl">🔒</span>
          </div>
          <h2 class="text-xl font-bold text-gray-900 dark:text-gray-100">加密过程</h2>
        </div>
        <div class="space-y-3 text-gray-700 dark:text-gray-300">
          <p><strong>输入：</strong>任意长度的明文字符串</p>
          <p><strong>处理：</strong></p>
          <ol class="list-decimal pl-5 space-y-2">
            <li>UTF-8 编码转换为字节数组</li>
            <li>使用零填充（补 0x00）至 16 字节的倍数</li>
            <li>AES-128-CBC 加密（密钥：<code class="bg-gray-100 dark:bg-gray-700 px-1 rounded">P.8CGq@Wr~Vs]!4!</code>）</li>
            <li>IV 与密钥相同</li>
            <li>输出 Base64 编码的密文</li>
          </ol>
          <div class="mt-4 p-3 bg-blue-100 dark:bg-blue-900/30 rounded-lg">
            <p class="text-sm font-medium text-blue-800 dark:text-blue-300">💡 提示：零填充要求明文长度必须是 16 字节的倍数</p>
          </div>
        </div>
      </div>

      <!-- 解密说明 -->
      <div class="bg-gradient-to-br from-green-50 to-emerald-50 dark:from-gray-900 dark:to-gray-800 rounded-2xl p-6">
        <div class="flex items-center gap-3 mb-4">
          <div class="w-10 h-10 bg-green-100 dark:bg-green-900 rounded-xl flex items-center justify-center">
            <span class="text-2xl">🔓</span>
          </div>
          <h2 class="text-xl font-bold text-gray-900 dark:text-gray-100">解密过程</h2>
        </div>
        <div class="space-y-3 text-gray-700 dark:text-gray-300">
          <p><strong>输入：</strong>Base64 编码的密文字符串</p>
          <p><strong>要求：</strong></p>
          <ul class="list-disc pl-5 space-y-2">
            <li>密文长度必须是 16 字节的倍数</li>
            <li>Base64 格式必须正确</li>
            <li>使用与加密时相同的密钥和 IV</li>
          </ul>
          <p><strong>处理：</strong></p>
          <ol class="list-decimal pl-5 space-y-2">
            <li>Base64 解码为字节数组</li>
            <li>AES-128-CBC 解密</li>
            <li>移除尾部的零填充字节（0x00）</li>
            <li>UTF-8 解码为原始字符串</li>
          </ol>
        </div>
      </div>
    </div>

    <!-- 技术规格 -->
    <div class="bg-gray-50 dark:bg-gray-900 rounded-2xl p-6 mb-8">
      <h2 class="text-xl font-bold text-gray-900 dark:text-gray-100 mb-4">技术规格</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <h3 class="font-semibold text-gray-800 dark:text-gray-200 mb-2">算法参数</h3>
          <div class="space-y-2 text-sm">
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">加密算法</span>
              <code class="font-mono text-gray-800 dark:text-gray-200">AES</code>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">密钥长度</span>
              <code class="font-mono text-gray-800 dark:text-gray-200">128位 (16字节)</code>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">工作模式</span>
              <code class="font-mono text-gray-800 dark:text-gray-200">CBC (密码块链接)</code>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">填充方式</span>
              <code class="font-mono text-gray-800 dark:text-gray-200">零填充 (Zero Padding)</code>
            </div>
          </div>
        </div>
        <div>
          <h3 class="font-semibold text-gray-800 dark:text-gray-200 mb-2">密钥信息</h3>
          <div class="space-y-2 text-sm">
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">密钥</span>
              <code class="font-mono text-gray-800 dark:text-gray-200 break-all">P.8CGq@Wr~Vs]!4!</code>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">初始化向量</span>
              <code class="font-mono text-gray-800 dark:text-gray-200 break-all">与密钥相同</code>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">字符集</span>
              <span class="text-gray-800 dark:text-gray-200">UTF-8</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600 dark:text-gray-400">输出编码</span>
              <span class="text-gray-800 dark:text-gray-200">Base64</span>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- 安全提示 -->
    <div class="bg-yellow-50 dark:bg-yellow-900/30 border border-yellow-200 dark:border-yellow-800 rounded-2xl p-6">
      <div class="flex items-start gap-3">
        <div class="flex-shrink-0">
          <span class="text-2xl">⚠️</span>
        </div>
        <div>
          <h3 class="text-lg font-semibold text-yellow-800 dark:text-yellow-300 mb-2">安全提示</h3>
          <ul class="space-y-2 text-yellow-700 dark:text-yellow-400">
            <li>• 所有加密/解密操作均在您的浏览器本地完成，数据不会上传到任何服务器</li>
            <li>• 此工具使用固定密钥（P.8CGq@Wr~Vs]!4!）</li>
            <li>• 零填充方式在某些场景下可能不够安全，建议用于学习或测试目的</li>
            <li>• 对于生产环境，请使用标准填充方式（如 PKCS7）和安全的密钥管理方案</li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</Layout>

<style>
  /* 自定义滚动条样式 */
  pre {
    overflow-x: auto;
  }
  
  pre::-webkit-scrollbar {
    height: 6px;
  }
  
  pre::-webkit-scrollbar-track {
    background: #f1f1f1;
    border-radius: 3px;
  }
  
  pre::-webkit-scrollbar-thumb {
    background: #888;
    border-radius: 3px;
  }
  
  pre::-webkit-scrollbar-thumb:hover {
    background: #555;
  }
  
  /* 暗色模式滚动条 */
  .dark pre::-webkit-scrollbar-track {
    background: #374151;
  }
  
  .dark pre::-webkit-scrollbar-thumb {
    background: #6b7280;
  }
  
  .dark pre::-webkit-scrollbar-thumb:hover {
    background: #9ca3af;
  }
</style>
