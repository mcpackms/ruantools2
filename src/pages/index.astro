---
// 页面元数据
const pageTitle = "RuanTools - 在线工具集合";
const description = "一个实用的在线工具集合，包含 JSON 格式化、Base64 编解码等常用开发工具。";

const tools = [
  {
    id: "json-formatter",
    name: "JSON 格式化工具",
    description: "格式化、验证和美化 JSON 数据，支持语法高亮和错误提示。",
    path: "/ruantools2/tools/json-formatter",
    category: "数据格式"
  },
  {
    id: "base64-url",
    name: "Base64 & URL 编解码",
    description: "支持 Base64 编码/解码和 URL 编码/解码，处理特殊字符和中文。",
    path: "/ruantools2/tools/base64-url",
    category: "编码转换"
  },
  {
    id: "timestamp",
    name: "时间戳转换",
    description: "Unix 时间戳与日期时间相互转换，支持多种格式。",
    path: "/ruantools2/tools/timestamp",
    category: "时间工具"
  },
  {
    id: "hash-generator",
    name: "哈希计算器",
    description: "计算文本的 MD5、SHA-1、SHA-256 等哈希值。",
    path: "/ruantools2/tools/hash",
    category: "加密工具"
  },
  {
    id: "color-converter",
    name: "颜色转换器",
    description: "RGB、HEX、HSL 颜色格式相互转换。",
    path: "/ruantools2/tools/color",
    category: "设计工具",
    comingSoon: true
  },
  {
    id: "regex-tester",
    name: "正则表达式测试",
    description: "在线测试和调试正则表达式。",
    path: "/ruantools2/tools/regex",
    category: "文本处理",
    comingSoon: true
  },
];
---

<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{pageTitle}</title>
    <style>
      :root {
        --primary: #2563eb;
        --primary-light: #3b82f6;
        --bg-color: #ffffff;
        --surface-color: #f8fafc;
        --border-color: #e2e8f0;
        --text-primary: #1e293b;
        --text-secondary: #64748b;
        --text-tertiary: #94a3b8;
        --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
        --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        --radius-md: 8px;
        --radius-lg: 12px;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        background-color: var(--bg-color);
        color: var(--text-primary);
        line-height: 1.5;
        min-height: 100vh;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 20px;
      }

      /* 头部样式 */
      .header {
        padding: 40px 0 30px;
        border-bottom: 1px solid var(--border-color);
        margin-bottom: 40px;
      }

      .site-title {
        font-size: 2rem;
        font-weight: 700;
        color: var(--primary);
        margin-bottom: 8px;
      }

      .site-subtitle {
        font-size: 1rem;
        color: var(--text-secondary);
        margin-bottom: 16px;
      }

      .site-meta {
        display: flex;
        gap: 20px;
        font-size: 0.875rem;
        color: var(--text-tertiary);
      }

      .meta-item {
        display: flex;
        align-items: center;
        gap: 6px;
      }

      .meta-item::before {
        content: "•";
        color: var(--primary-light);
      }

      /* 工具列表样式 */
      .tools-section {
        margin-bottom: 60px;
      }

      .section-title {
        font-size: 1.25rem;
        font-weight: 600;
        color: var(--text-primary);
        margin-bottom: 24px;
        padding-bottom: 12px;
        border-bottom: 2px solid var(--border-color);
      }

      .tools-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
        gap: 20px;
      }

      @media (max-width: 768px) {
        .tools-grid {
          grid-template-columns: 1fr;
        }
      }

      /* 工具卡片 - 简洁版 */
      .tool-item {
        background: var(--surface-color);
        border: 1px solid var(--border-color);
        border-radius: var(--radius-md);
        padding: 20px;
        transition: all 0.2s ease;
        text-decoration: none;
        color: inherit;
        display: block;
      }

      .tool-item:hover {
        border-color: var(--primary-light);
        box-shadow: var(--shadow-md);
        transform: translateY(-2px);
      }

      .tool-item.coming-soon {
        opacity: 0.7;
        cursor: not-allowed;
      }

      .tool-item.coming-soon:hover {
        border-color: var(--border-color);
        box-shadow: none;
        transform: none;
      }

      .tool-header {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        margin-bottom: 12px;
      }

      .tool-name {
        font-size: 1.125rem;
        font-weight: 600;
        color: var(--text-primary);
      }

      .tool-category {
        font-size: 0.75rem;
        font-weight: 500;
        color: var(--primary);
        background: rgba(37, 99, 235, 0.1);
        padding: 4px 8px;
        border-radius: 4px;
      }

      .tool-description {
        font-size: 0.875rem;
        color: var(--text-secondary);
        margin-bottom: 16px;
        line-height: 1.6;
      }

      .tool-status {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .tool-link {
        font-size: 0.875rem;
        font-weight: 500;
        color: var(--primary);
        text-decoration: none;
      }

      .tool-link:hover {
        text-decoration: underline;
      }

      .coming-soon-badge {
        font-size: 0.75rem;
        color: var(--text-tertiary);
        font-style: italic;
      }

      /* 页脚样式 */
      .footer {
        padding: 40px 0;
        border-top: 1px solid var(--border-color);
        margin-top: 60px;
        text-align: center;
      }

      .footer-content {
        max-width: 600px;
        margin: 0 auto;
      }

      .footer-text {
        font-size: 0.875rem;
        color: var(--text-secondary);
        margin-bottom: 16px;
      }

      .footer-links {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-top: 20px;
      }

      .footer-link {
        font-size: 0.875rem;
        color: var(--primary);
        text-decoration: none;
      }

      .footer-link:hover {
        text-decoration: underline;
      }

      /* 使用说明 */
      .usage-note {
        background: rgba(37, 99, 235, 0.05);
        border: 1px solid rgba(37, 99, 235, 0.2);
        border-radius: var(--radius-md);
        padding: 16px;
        margin-top: 30px;
      }

      .usage-title {
        font-size: 0.875rem;
        font-weight: 600;
        color: var(--primary);
        margin-bottom: 8px;
      }

      .usage-text {
        font-size: 0.875rem;
        color: var(--text-secondary);
        line-height: 1.5;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <!-- 网站头部 -->
      <header class="header">
        <h1 class="site-title">RuanTools</h1>
        <p class="site-subtitle">简洁实用的在线工具集合，专为开发者设计</p>
        <div class="site-meta">
          <span class="meta-item">本地运行 · 无需上传</span>
          <span class="meta-item">完全免费 · 无需注册</span>
          <span class="meta-item">持续更新 · 开源项目</span>
        </div>
      </header>

      <!-- 主要工具区域 -->
      <main>
        <section class="tools-section">
          <h2 class="section-title">可用工具</h2>
          <div class="tools-grid">
            {tools.filter(t => !t.comingSoon).map((tool) => (
              <a href={tool.path} class="tool-item">
                <div class="tool-header">
                  <h3 class="tool-name">{tool.name}</h3>
                  <span class="tool-category">{tool.category}</span>
                </div>
                <p class="tool-description">{tool.description}</p>
                <div class="tool-status">
                  <span class="tool-link">立即使用 →</span>
                </div>
              </a>
            ))}
          </div>
        </section>

        <!-- 即将上线 -->
        <section class="tools-section">
          <h2 class="section-title">即将上线</h2>
          <div class="tools-grid">
            {tools.filter(t => t.comingSoon).map((tool) => (
              <div class="tool-item coming-soon">
                <div class="tool-header">
                  <h3 class="tool-name">{tool.name}</h3>
                  <span class="tool-category">{tool.category}</span>
                </div>
                <p class="tool-description">{tool.description}</p>
                <div class="tool-status">
                  <span class="coming-soon-badge">开发中</span>
                </div>
              </div>
            ))}
          </div>
        </section>

        <!-- 使用说明 -->
        <div class="usage-note">
          <div class="usage-title">使用说明</div>
          <p class="usage-text">
            所有工具均在您的浏览器本地运行，数据不会上传到任何服务器。
            如果您发现任何问题或有新工具建议，欢迎在 GitHub 仓库提交 Issue。
          </p>
        </div>
      </main>

      <!-- 页脚 -->
      <footer class="footer">
        <div class="footer-content">
          <p class="footer-text">
            © 2024 RuanTools · 基于 Astro 构建 · 托管于 GitHub Pages
          </p>
          <p class="footer-text">
            项目开源地址：https://github.com/mcpackms/ruantools2
          </p>
          <div class="footer-links">
            <a href="https://github.com/mcpackms/ruantools2" class="footer-link" target="_blank">GitHub</a>
            <a href="https://github.com/mcpackms/ruantools2/issues" class="footer-link" target="_blank">问题反馈</a>
            <a href="https://astro.build" class="footer-link" target="_blank">Astro 框架</a>
          </div>
        </div>
      </footer>
    </div>

    <script>
      // 为即将上线的工具添加提示
      document.addEventListener('DOMContentLoaded', function() {
        const comingSoonItems = document.querySelectorAll('.tool-item.coming-soon');
        comingSoonItems.forEach(item => {
          item.addEventListener('click', function(e) {
            e.preventDefault();
            alert('该工具正在开发中，即将上线！');
          });
        });
      });
    </script>
  </body>
</html>
